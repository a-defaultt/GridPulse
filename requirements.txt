# Feed parsing
feedparser>=6.0.0

# HTTP client
requests>=2.31.0

# Timezone handling
pytz>=2024.1
python-dateutil>=2.9.0

# HTML parsing (vendor scraping)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Config parsing
PyYAML>=6.0.1
python-dotenv>=1.0.0

# Template rendering
Jinja2>=3.1.4

# Scheduling
schedule>=1.2.1

# LLM Standard Client (V5 enhancement for batching via NVIDIA/Groq)
openai>=1.14.0
numpy>=1.26.0

# Cryptography
python-gnupg>=0.5.2

# Mallory.ai threat-intel SDK (IOC feed + enrichment)
malloryapi>=0.3.0

# Google Sheets sync (service-account write access)
gspread>=6.1.0

# Charts (uncomment if using chart_generator)
# matplotlib>=3.9.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Note: stdlib modules (no pip install needed):
# os, sqlite3, logging, pathlib, datetime, typing, smtplib, json, re, html
